import { injectable } from 'inversify';

/**
 * LoginUseCase — business logic interactor for login flow.
 * Placeholder: wire up when complex multi-repository login logic is needed.
 */
@injectable()
export class LoginUseCase {
  async execute(_email: string, _password: string): Promise<void> {
    // Placeholder: implement login business logic here
  }
}
