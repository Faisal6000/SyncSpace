import React from 'react';
import { View } from 'react-native';

/**
 * Portal for global modal/dialog rendering.
 * Add dialog components here as needed.
 */
export default function Dialogs() {
  return <View />;
}
