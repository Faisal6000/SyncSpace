/**
 * NavigationContainer is now rendered inside ThemeWrapper.
 * This file is intentionally empty — it exists only as a placeholder
 * to document that NavigationContainer wrapping is handled in:
 * src/utils/theme/ThemeWrapper.tsx
 *
 * The navigationRef lives in:
 * src/domain/services/NavigationService.ts
 */
export {};
