/**
 * Permission check functions.
 * Implement permission logic here when real ACL is wired up.
 */
export function checkPermission(permission: string, _userPermissions: string[] = []): boolean {
  // Stub: always returns true
  return true;
}
